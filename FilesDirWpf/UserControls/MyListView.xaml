<UserControl x:Class="FilesDirWpf.UserControls.MyListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    
    <Border Margin="0, 0, 5, 0"
            CornerRadius="3"
            BorderBrush="{StaticResource RgbM2}"
            BorderThickness="2">
    <Grid Margin="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="25"/>
        </Grid.ColumnDefinitions>
        
        <ListBox Grid.Column="0" x:Name="LstBx" 
                 Margin="0, 0, 5, 0"
                 SelectionMode="Extended"/>
        
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="25"/>
                <RowDefinition Height="25"/>
                <RowDefinition Height="25"/>
                <RowDefinition Height="25"/>
            </Grid.RowDefinitions>
            
            <Button Grid.Row="0" x:Name="BtnPlus" Height="25" Click="BtnPlus_OnClick">
                <Image Style="{StaticResource ImgPlusOk}"
                       Width="16"
                       Height="16" />
            </Button>
            <Button Grid.Row="1" x:Name="BtnMinus" Height="25" Click="BtnMinus_OnClick">
                <Image Style="{StaticResource ImgMinusNok}"
                       Width="16"
                       Height="16" />
            </Button>
            <Button Grid.Row="2" x:Name="BtnEdit" Height="25" Click="BtnEdit_OnClick">
                <Image Style="{StaticResource ImgEditM2}"
                       Width="16"
                       Height="16" />
            </Button>
            <Button Grid.Row="3" x:Name="BtnCleen" Height="25" Click="BtnCleen_OnClick">
                <Image Style="{StaticResource ImgClear}"
                       Width="16"
                       Height="16" />
            </Button>
        </Grid>
        
    </Grid>
    </Border>
    
</UserControl>
